# lab_c_tutor

A minimal C project using CMake.

## Build

```sh
mkdir -p build
cd build
cmake ..
cmake --build .
```

## Run

```sh
./lab_c_tutor
```

## Test

```sh
cd build
ctest --output-on-failure
```
